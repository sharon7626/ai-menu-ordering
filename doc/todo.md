# AI 菜單點餐系統開發待辦

## 使用方式

- 依序完成任務，每項完成後再進入下一項。
- 每個任務需通過列出的驗收方式才可勾選。
- 第一階段須保持不依賴 AI 功能也能獨立運作。
- 未勾選任務不得提前實作；產品方向改變時，先更新需求與任務順序再繼續開發。

## 第一階段：點餐底座

- [x] **1. 建立 `menu.json` 資料格式與範例資料（Done）**
  - 定義分類、品項識別碼、品項名稱與價格欄位。
  - 建立至少兩個分類及每類至少兩個品項的範例。
  - 驗收：檔案為合法 JSON；所有品項具備唯一識別碼、名稱與非負整數價格；格式能表達分類與其品項。
  - 對應：FR-01、AC-01。

- [x] **2. 建立靜態點餐頁面骨架（Done）**
  - 建立基本 HTML 結構及必要的 CSS、JavaScript 檔案入口，不使用 React 或 Vite。
  - 驗收：瀏覽器可直接開啟頁面，頁面具備菜單區與購物車區的明確容器，且無載入錯誤。
  - 對應：FR-02。

- [x] **3. 讀取並顯示 `menu.json`（Done）**
  - 依分類渲染品項名稱與價格。
  - 驗收：範例資料中的所有分類及品項皆正確顯示，價格顯示一致。
  - 對應：FR-01、FR-02、AC-01。

- [x] **4. 實作品項數量調整（Done）**
  - 支援加入多個品項、增加數量、減少數量，數量不得小於零。
  - 驗收：可同時選取至少兩種品項並設定不同數量；反覆增減後數量正確。
  - 對應：FR-03、AC-02。

- [x] **5. 實作購物車明細與總金額（Done）**
  - 顯示已選品項、單價、數量、小計及總金額。
  - 驗收：新增、減少或移除品項時明細與總金額立即正確更新。
  - 對應：FR-04、AC-03。

- [x] **6. 建立 FastAPI 專案骨架與靜態檔案服務（Done）**
  - 建立最小可啟動的後端結構，提供點餐頁面與靜態資源。
  - 驗收：本機啟動後可由瀏覽器開啟點餐頁面，且 `menu.json` 可正常載入。
  - 對應：FR-01、FR-08。

- [x] **7. 定義訂單資料格式與送出 API（Done）**
  - 訂單包含顧客姓名、品項識別碼、品項名稱、單價、數量、小計、總金額與建立時間。
  - 驗收：缺少姓名、購物車為空、數量無效或金額不一致時拒絕請求；有效訂單回傳成功結果。
  - 對應：FR-05、AC-04。

- [x] **8. 建立 SQLite 訂單資料表（Done）**
  - 設計訂單與訂單明細資料表，並建立初始化方式。
  - 驗收：全新本機環境可建立資料表；資料表可保存一張訂單及多筆明細。
  - 對應：FR-06。

- [x] **9. 將有效訂單儲存至 SQLite（Done）**
  - 串接送出訂單 API 與資料庫寫入流程。
  - 驗收：送出有效訂單後，可從 SQLite 查到顧客姓名、各品項數量與正確總金額；寫入失敗不留下不完整訂單。
  - 對應：FR-06、AC-05。

- [x] **10. 完成顧客姓名輸入與送單流程（Done）**
  - 前端提供姓名欄位、必要驗證、送出狀態與結果提示。
  - 驗收：未填姓名或購物車為空時不可送出；有效資料可成功送出且避免重複點擊送單。
  - 對應：FR-05、AC-04。

- [x] **11. 建立管理者訂單查詢 API（Done）**
  - 提供訂單列表與訂單明細資料。
  - 驗收：API 可讀取多張訂單並回傳顧客姓名、建立時間、總金額及完整品項明細。
  - 對應：FR-07。

- [x] **12. 建立簡易管理後台訂單頁面（Done）**
  - 顯示訂單列表與各訂單內容，不新增未確認的管理功能。
  - 驗收：管理者可看到 SQLite 中的訂單、顧客姓名、品項、數量及總金額。
  - 對應：FR-07、AC-06。

- [x] **13. 執行第一階段整合驗收（Done）**
  - 按照 AC-01 至 AC-07 完整測試點餐流程。
  - 驗收：七項條件全部通過，並確認未設定或未串接 AI 時系統仍可獨立使用。
  - 對應：AC-01 至 AC-07。

## 第二階段：AI 菜單匯入

- [x] **14. 定義上傳檔案規則與錯誤訊息（Done）**
  - 僅接受 JPG、PNG 及一頁式 PDF；單一檔案大小上限為 10 MB。
  - 驗收：允許的格式可進入處理流程，不允許的格式會收到明確錯誤訊息。
  - 對應：FR-09。

- [x] **15. 建立菜單檔案上傳介面與 API（Done）**
  - 提供單一菜單檔案的選擇、上傳與基本驗證。
  - 驗收：三種允許格式皆可成功上傳；多頁 PDF 與不允許格式會被拒絕。
  - 對應：FR-09。

- [x] **16. 確認 AI API 方案與結構化輸出契約（Done）**
  - 選定供應商與模型，定義分類、品項名稱與價格的輸出結構及錯誤處理方式。
  - 驗收：決策與資料契約完成文件化，且輸出可轉換為第一階段的 `menu.json` 格式。
  - 對應：FR-10。

- [x] **17. 串接 AI 菜單辨識流程（Done）**
  - 將允許的檔案交由 AI 辨識並解析結構化結果。
  - 驗收：以清楚的測試菜單可取得分類、品項名稱及價格；失敗時不寫入正式菜單並顯示可理解的錯誤。
  - 對應：FR-10。

- [x] **18. 建立辨識結果確認與修改介面（Done）**
  - 店家可修改分類、品項名稱及價格，再進行確認。
  - 驗收：修改後畫面與送出資料一致；無效或缺漏資料不可確認。
  - 對應：FR-11。

- [x] **19. 確認雙模式產品方向與資料歸屬（Done）**
  - 將產品分成優先開發的團購模式，以及後續店家固定菜單模式；兩者共用菜單、購物車與訂單核心。
  - 驗收：需求文件清楚區分團購代碼、統籌管理連結、個人訂單編號、團購菜單快照與店家固定菜單，且不再將 `data/menu.json` 視為全站正式菜單。
  - 對應：FR-14 至 FR-26。

## 第三階段：一般使用者團購模式

- [x] **20. 定義團購資料格式與安全規則**（Done）
  - 定義團購、菜單快照、團購代碼、管理 Token、狀態與個人訂單的欄位及驗證規則。
  - 驗收：資料格式可表達一個團購、多位參與者與多張訂單，並明確區分公開代碼與私密管理 Token。
  - 對應：FR-14、FR-15、FR-17、FR-20。

- [x] **21. 建立 SQLite 團購資料表**（Done）
  - 建立團購、團購菜單及必要關聯，讓現有訂單可以歸屬於特定團購。
  - 驗收：全新資料庫可建立兩個互不影響的團購，且各自保存菜單與訂單關聯。
  - 對應：FR-14、FR-19。

- [x] **22. 將確認結果建立為團購菜單**（Done）
  - 把人工確認後的辨識結果轉成統一菜單格式，建立團購菜單快照、團購代碼與統籌管理連結。
  - 驗收：建立成功後回傳公開團購代碼及獨立管理連結；不修改示範用 `data/menu.json`。
  - 對應：FR-14、FR-15、AC-08。

- [x] **23. 建立團購代碼進入頁面**（Done）
  - 參與者可輸入團購代碼或開啟分享連結，進入該次團購菜單。
  - 驗收：正確代碼顯示正確菜單；不存在或已關閉的代碼顯示容易理解的訊息。
  - 對應：FR-16。

- [x] **24. 將個人訂單連結至團購**（Done）
  - 沿用姓名、購物車與送單流程，為有效訂單保存團購關聯並產生個人訂單編號。
  - 驗收：至少兩位參與者可在同一團購送單並取得不同編號，訂單不會寫到其他團購。
  - 對應：FR-16、FR-17、AC-09。

- [x] **25. 建立參與者個人訂單頁面**（Done）
  - 送單後顯示自己的訂單編號、姓名、餐點、數量與金額。
  - 驗收：參與者能查看自己的訂單，且公開團購代碼不能取得其他人的完整明細。
  - 對應：FR-17、FR-18、AC-10。

- [x] **26. 建立統籌團購管理頁面**（Done）
  - 使用管理連結查看所有參與者的個人訂單，不以公開團購代碼提供管理權限。
  - 驗收：管理連結可查看正確團購的全部個人明細；錯誤 Token 不可存取。
  - 對應：FR-18。

- [x] **27. 建立餐點彙整統計**（Done）
  - 統籌頁面依餐點彙整總數量與金額，同時保留依參與者查看方式。
  - 驗收：多位參與者重複點相同品項時，品項總數量與總金額計算正確。
  - 對應：FR-19、AC-11。

- [x] **28. 建立關閉團購與文字摘要**（Done）
  - 統籌可關閉團購並複製適合傳訊息或電話訂餐使用的純文字摘要。
  - 驗收：關閉後拒絕新訂單，既有明細仍可查看，複製內容與畫面統計一致。
  - 對應：FR-20、FR-21、AC-12。

- [x] **29. 執行團購模式整合驗收**（Done）
  - 從上傳菜單、建立團購、多人送單到統籌彙整與關閉，完整測試團購流程。
  - 驗收：AC-08 至 AC-12 全部通過，且既有固定示範菜單點餐方式仍可運作。
  - 對應：AC-08 至 AC-12。

## 第四階段：店家固定菜單模式

- [x] **30. 定義店家固定菜單資料格式與入口**（Done）
  - 定義單一店家、固定菜單、公開網址、管理入口與 QR Code 的資料關係。
  - 驗收：文件能清楚區分店家固定菜單與團購菜單快照，且管理資訊不進入公開 QR Code。
  - 對應：FR-22、FR-23。

- [x] **31. 建立店家固定菜單儲存與更新**（Done）
  - 讓店家將確認結果保存為自己的固定菜單，並能在之後更新。
  - 驗收：更新後公開頁顯示新菜單，既有訂單仍保留送單當時名稱與價格。
  - 對應：FR-22。

- [x] **32. 建立店家固定網址與 QR Code**（Done）
  - 顧客透過固定網址或 QR Code 進入該店點餐頁。
  - 驗收：掃描或開啟網址後只顯示該店固定菜單，QR Code 不包含管理資訊。
  - 對應：FR-23、FR-24、AC-13。

- [x] **33. 建立店家顧客個人訂單流程**（Done）
  - 沿用購物車與姓名流程，送單後產生個人訂單編號並提供自己的訂單內容。
  - 驗收：至少兩位顧客可取得不同編號，並分別看到正確餐點。
  - 對應：FR-24、FR-25、AC-14。

- [x] **34. 建立店家訂單後台**（Done）
  - 店家依個人訂單編號與姓名查看每位顧客的餐點及數量。
  - 驗收：店家能明確辨識每份餐點屬於哪位顧客，不混入其他情境的訂單。
  - 對應：FR-26、AC-15。

- [x] **35. 執行店家模式與雙模式整合驗收**（Done）
  - 完整測試店家固定菜單流程，並確認團購模式仍可獨立運作。
  - 驗收：AC-13 至 AC-15 通過，且兩種模式的菜單與訂單互不混淆。
  - 對應：AC-13 至 AC-15。

## 第五階段：發布前操作體驗補強

- [x] **36. 建立雙模式系統首頁**（Done）
  - 首頁清楚提供建立團購、使用代碼加入團購及建立店家固定菜單入口；早期固定菜單移至獨立示範頁。
  - 驗收：三個主要入口皆前往正確流程，團購與店家建立流程都能上傳菜單並進行 AI 辨識。
  - 對應：FR-27、AC-16。

- [x] **36A. 修正多價格辨識與團購入口流程**（Done）
  - 明確辨識 L／XL、大小份等價格欄並拆成獨立可點項目；首頁依主揪、參與者與店家角色分流。
  - 驗收：多價格不再因規格欄而全部留空；主揪知道先上傳建立團購，參與者可在首頁直接輸入 6 碼代碼，建立成功頁清楚提供分享與管理入口。
  - 對應：FR-10、FR-27、AC-16。

- [x] **36B. 重整團購選品、點餐與彙整體驗**（Done）
  - 團購確認頁鎖定 AI 菜名與價格，只勾選本次提供品項；點餐頁改用緊湊清單並按需展開備註。
  - 驗收：同品項不同備註會分開彙整；管理頁個人明細只以姓名為標題；桌面及手機點餐不會預先展開所有備註欄。
  - 對應：FR-11、FR-18、FR-19、FR-31、AC-11、AC-20。

- [x] **36C. 精簡菜單設定與團購輸出介面**（Done）
  - 團購確認頁改為緊湊勾選清單，提供品項名稱、分類與最高價格篩選；移除沒有後續頁面的步驟編號。
  - 同品項不同備註在團購管理頁相鄰分組；新增含餐點彙整與個人明細的 Excel 下載；店家確認頁將 L／XL 同品項同列呈現。
  - 驗收：三種篩選與批次勾選可用；同品項需求顯示於同一品項卡；Excel 可開啟且不含管理 Token；首頁及店家規格排列在電腦與手機可閱讀。
  - 對應：FR-11、FR-19、FR-21、FR-27、FR-29、AC-11、AC-12、AC-16、AC-18、AC-20。

- [x] **37. 補齊主要頁面的返回與首頁導覽**（Done）
  - 在上傳、加入團購、點餐、個人訂單及管理等分頁最上方加入單一「首頁」按鈕；首頁保留角色入口卡片，不額外顯示固定示範菜單。
  - 驗收：8 個主要分頁在手機與電腦都能從頁首返回首頁，私密管理連結不會被加入公開網址或 QR Code。
  - 對應：FR-28、AC-17。

- [x] **38. 建立兩種模式的餐點備註**（Done）
  - 團購參與者及店家顧客可為每個已選餐點填寫選填的自由文字備註，並在個人訂單及對應管理後台顯示。
  - 驗收：有備註與空白備註皆可正常送單；備註最多 200 字，不改變菜單單價，也不建立固定糖度、冰量、辣度或加料選項。
  - 對應：FR-30、AC-19。

- [x] **39. 建立店家固定菜單分類與品項增刪**（Done）
  - 店家建立或更新固定菜單時可新增、刪除分類與品項；團購模式不顯示增刪控制。
  - 驗收：店家增刪後保存的公開菜單與確認畫面一致，且既有訂單內容不受影響。
  - 對應：FR-29、AC-18。

- [x] **40. 團購 Excel 新增訂購者合計**（Done）
  - Excel 新增「訂購者合計」工作表，每張送出的訂單各列一筆姓名、餐點總數量、合計金額與送單時間。
  - 驗收：各列數量及金額與實際訂單一致；同名的不同訂單不合併；餐點彙整與個人明細仍保留完整資料。
  - 對應：FR-21、AC-12。

- [x] **41. 建立店家品項彙整與 Excel 輸出**（Done）
  - 店家後台新增依品項及備註需求彙整，並保留個別顧客訂單；有訂單時可下載店家 Excel。
  - 驗收：多位顧客的同品項數量及金額正確加總，不同備註分列於同品項下；Excel 含餐點彙整、顧客合計與顧客明細，錯誤管理 Token 不可下載。
  - 對應：FR-26、AC-15。

- [x] **42. 允許補填未辨識的餐廳名稱**（Done）
  - AI 未辨識到餐廳名稱時，團購與店家確認頁可由使用者手動輸入名稱；已有辨識名稱時維持原本模式規則。
  - 驗收：空白名稱顯示明確提示且不可直接建立，手動輸入後可通過確認；團購餐點名稱與價格仍不可修改。
  - 對應：FR-11、AC-20。

- [x] **43. 改善 Gemini 菜單辨識穩定性與延遲**（Done）
  - Gemini 3.6 Flash 使用低思考層級；一般 429、5xx、HTTP 逾時與傳輸錯誤最多重試 2 次，等待約 1 秒與 2 秒。
  - 驗收：429 或 503 後可重試成功；重試耗盡才回既有安全錯誤；不可恢復錯誤不重試，單次請求維持 60 秒逾時。
  - 對應：FR-10、FR-12。

- [x] **43A. 加入 Gemini 安全診斷紀錄與 OSError 重試**（Done）
  - 每次呼叫在後端 console 記錄 attempt、開始時間、結果、耗時及安全錯誤類型；不記錄錯誤原文、機密或上傳內容。
  - 驗收：OSError 可重試後成功；log 可看到 attempt 與耗時，且不包含測試錯誤原文。
  - 對應：FR-12、FR-13。

- [x] **43B. 依 Gemini 結構化錯誤修正額度判斷與重試等待**（Done）
  - 只有 quota ID 明確包含 PerDay／Daily 才判定每日額度用完；FreeTier 本身不再作為每日額度判斷。
  - 一般 RPM／TPM 429 優先使用 Gemini RetryInfo 或 HTTP Retry-After，沒有供應商等待時間時才使用約 1 秒、2 秒的遞增備援。
  - 驗收：代表性真實菜單在未受網路沙箱限制的環境完成 5 次 Gemini 呼叫並保存安全耗時紀錄；結構化 PerDay、RPM、TPM 與 retry delay 測試通過。
  - 對應：FR-12、FR-13。

- [x] **44. 將所有同品項不同規格合併為同一點餐列**（Done）
  - 不限飲料或 L／XL；凡名稱可判定為同一品項、只是規格不同，都在同一列顯示，且各規格仍可獨立選擇數量與備註。
  - 驗收：以至少一份非飲料多規格菜單測試，同品項各規格同列且不會錯誤合併不同商品。
  - 對應：FR-11、FR-29、FR-31、AC-18、AC-20。

## 第六階段：GitHub + Render 正式部署

- [x] **45. 完成 Render deployment-ready 設定**（Done）
  - 確認 FastAPI 入口、Python 版本、完整依賴、靜態資源、Build／Start Command、Environment Variables、QR Code 正式網址及 Excel 無本機路徑依賴。
  - 驗收：Uvicorn 可從正式入口啟動；首頁、前端與菜單資料 HTTP 回應正常；部署設定與機密規則有文件及自動測試。

- [x] **46. 建立 SQLite／PostgreSQL 雙環境資料層**（Done）
  - 本機維持 SQLite；`DATABASE_URL` 為 Render PostgreSQL 時使用 Psycopg、正式空庫建表與 PostgreSQL 交易鎖。
  - 驗收：SQLite 完整流程不變；PostgreSQL URL、參數轉換、主鍵回傳、建表及交易差異相容性測試通過。

- [x] **47. 建立 Private GitHub Repository 並完成 Render 真實部署驗收**（Done）
  - 已建立 Private GitHub Repository、Render PostgreSQL 與 Web Service，正式網址為 `https://ai-menu-ordering.onrender.com`。
  - 驗收：使用者已在 Render 公開網址完成 Gemini、三人團購、團購 Excel、店家固定菜單、QR Code、兩筆店家訂單與店家 Excel；服務重啟後團購與菜單資料仍可讀取，手機公開店家頁修正後亦完成驗收。

## 第七階段：選擇性登入與品牌體驗升級

- [x] **48. 建立 Firebase Authentication 設定與後端 ID Token 驗證**（Done）
  - 加入不含正式值的 Firebase 環境設定、前端 Google Sign-In 初始化，以及 FastAPI 的 Firebase ID Token 驗證層。
  - 驗收：Google 登入、登出與狀態維持可用；後端只信任驗證後的 Firebase UID，缺少、偽造、過期或錯誤專案 Token 皆拒絕；未登入訪客功能不受影響。
  - 對應：FR-32、FR-33、FR-41、AC-21、AC-22。

- [x] **49. 建立使用者資料表與既有資料的選填關聯**（Done）
  - 為 SQLite 與 PostgreSQL 新增 `app_users`，並為團購加入可為 `NULL` 的 `owner_user_id`、訂單加入可為 `NULL` 的 `user_id`。
  - 驗收：新舊資料庫都可安全初始化；既有團購、訂單、Token、Excel 與訪客流程完全不變，沒有任何舊資料被自動指派給帳號。
  - 對應：FR-34、FR-35、FR-36、AC-21。

- [x] **50. 將登入狀態安全綁定至新團購與新訂單**（Done）
  - 登入建立團購時綁定 owner；登入送出團購或店家訂單時綁定 user；訪客仍保存 `NULL`。
  - 驗收：登入與訪客各完成一組建立及送單流程，後端只依驗證 UID 綁定，不接受前端自行指定使用者。
  - 對應：FR-35、FR-36、AC-21、AC-23、AC-24。

- [x] **51. 建立「我的團購」與帳號擁有者管理入口**（Done）
  - 登入後列出本人建立或 Claim 的團購，並以登入身分安全取得管理資料；保留既有 management Token 管理入口。
  - 驗收：同帳號跨瀏覽器登入可找到團購；帳號 A 無法查看或管理帳號 B 的團購，公開團購碼不具管理權限。
  - 對應：FR-37、AC-23、AC-27。

- [x] **52. 建立「我的訂單」與帳號擁有者查看入口**（Done）
  - 登入後列出本人送出或 Claim 的團購／店家訂單，並以登入身分安全查看；保留既有 order view Token 入口。
  - 驗收：同帳號跨瀏覽器登入可找到訂單；帳號 A 無法查看帳號 B 或訪客的私人訂單。
  - 對應：FR-38、AC-24、AC-27。

- [x] **53. 建立訪客團購與訂單的安全 Claim**（Done）
  - 訪客登入後，只有持有有效 management Token 才能 Claim 團購，持有有效 order view Token 才能 Claim 訂單。
  - 驗收：正確 Token 可綁定；錯誤 Token、只有團購碼、姓名或訂單編號、以及已屬於其他帳號時皆拒絕，不洩漏資料是否存在或擁有者資訊。
  - 對應：FR-39、FR-40、AC-25、AC-26。

- [x] **54. 評估並建立「我的菜單」最小可行版本**（Done）
  - 先確認能否在不重構既有菜單快照與店家資料模型的前提下，保存常用已確認菜單並再次建立團購。
  - 驗收：若可用小範圍修改完成，登入使用者可從本人菜單再次建立團購且不重新呼叫 Gemini；若需大幅重構，將評估結果與後續資料規格文件化並停止實作。
  - 對應：使用者確認優先順序 1～4；不得延誤 FR-32 至 FR-40。

- [x] **55. 建立全站共用 Design System**（Done）
  - 建立共用 CSS Variables 與 button、input、select、surface、card、badge、nav、modal、toast、loading、empty state、focus 及 motion 元件。
  - 驗收：主要頁面可引用同一套 token 與元件，不再各自複製核心色彩與互動；既有功能與頁面載入維持正常。
  - 對應：FR-42、FR-45、FR-47、AC-28。

- [x] **56. 重新設計首頁品牌構圖與輕量互動**（Done）
  - 依確認資訊架構重整 Hero、四步驟、店家入口與登入價值區，使用原生 CSS／JavaScript 建立收斂的游標、漂浮與 reveal 效果。
  - 驗收：主要 CTA 清楚；桌面有柔和互動，手機與 reduced-motion 自然降級，沒有 scroll hijacking、假 loading、遮擋或大型動畫套件。
  - 對應：FR-43、FR-44、FR-45、FR-46、FR-47、AC-28、AC-29。

- [x] **57. 套用品牌系統至上傳、點餐、訂單與管理頁**（Done）
  - 依任務強度逐頁收斂互動，保留同品項 variants、購物車、Excel、QR Code、Token 權限及所有既有操作。
  - 驗收：上傳、確認、團購／店家點餐、個人訂單、我的頁面與管理頁風格一致；長文字、多規格、loading、empty、error 在桌面與手機可用。
  - 對應：FR-42、FR-44、FR-46、FR-48、AC-28、AC-29。

- [x] **58. 執行帳號安全、完整回歸、UI、效能與無障礙驗收**（Done）
  - 驗證訪客、登入、跨瀏覽器、帳號隔離與 Claim；重跑 Gemini、圖片／PDF、crop、團購、店家、Excel、QR Code、PostgreSQL 相容性與全部既有測試。
  - [x] 我的團購與我的訂單已加入可恢復的封存清單；封存不刪除原始資料，且再次開團沿用「我的菜單」既有功能。
  - 驗收：AC-21 至 AC-31 全部通過；375、390、430、768 與桌面無溢出，鍵盤、焦點、對比、ARIA、觸控與 reduced motion 通過，正式載入預算未明顯退步。
  - 對應：AC-21 至 AC-30。

- [x] **59. 整理 migration、Firebase、Render 與 Production 部署前清單**（Done）
  - 說明 Firebase Console、Render Environment Variables、PostgreSQL migration、GitHub push 後行為與正式驗收順序。
  - 驗收：程式、migration 與測試均完成；使用者可依一次一步的指示設定，但尚未 commit、push 或部署新版 Production。
  - 對應：正式部署限制與安全規範。

- [x] **60. 將店家固定菜單納入「我的菜單」並提供安全管理入口**（Done）
  - 登入建立店家固定菜單時記錄帳號擁有者，在「我的菜單」以「店家固定菜單」標示，並提供調整菜單、店家訂單與公開點餐頁入口。
  - 舊固定菜單不依店名或公開網址猜測擁有者；只有登入者持有原完整管理 Token 時才能安全認領。
  - 驗收：帳號 A 可跨裝置列出及更新自己的固定菜單；帳號 B 無法查看或更新；舊 Token 管理連結、店家訂單、Excel、QR Code 與訪客流程維持可用。
  - 對應：FR-22、FR-32、FR-34、FR-40。

- [x] **61. 在「我的訂單」集中呈現本人店家與送出訂單**（Done）
  - 將頁面分為「我的店家固定菜單」及「我送出的訂單」，店家卡片提供調整菜單、店家訂單及顧客點餐入口。
  - 舊店家可在同頁貼入原完整管理連結安全認領；只曾向店家送單不會自動取得管理權限。
  - 驗收：本人新建或已認領的固定菜單可在我的訂單看到；另一帳號不可列出或認領；個人訂單、封存、Excel、QR Code 與歷史資料不受影響。
  - 對應：FR-22、FR-38、FR-50、AC-32。

- [x] **62. 在「我的菜單」提供舊固定菜單安全找回與調整入口**（Done）
  - 「我的菜單」同時呈現團購常用菜單及本人店家固定菜單；固定菜單明確標示並提供調整、店家訂單與公開點餐頁入口。
  - 舊固定菜單可貼入原完整管理連結安全認領，成功後立即出現在清單；不依店名、訂單或公開網址猜測擁有者。
  - 驗收：正確 management Token 可加入本人菜單並進入調整；錯誤 Token 或其他帳號既有店家不可認領，既有菜單、訂單、Excel 與 QR Code 不受影響。
  - 對應：FR-22、FR-51、AC-33。

- [x] **63. 統一店家固定菜單的兩種更新方式**（Done）
  - 從建立完成、店家訂單後台或「我的菜單」進入調整時，先載入目前固定菜單供直接增刪修改，也可切換為上傳全新菜單重新辨識。
  - [x] 修正巢狀更新網址誤載相對路徑資源的回歸問題；三個入口現在都能執行共用更新程式並顯示目前菜單。
  - 驗收：三個入口使用同一更新頁；直接修改與全新上傳皆沿用既有安全權限及固定公開網址，歷史訂單、Excel 與 QR Code 不受影響。
  - 對應：FR-22、FR-29、FR-52、AC-34。

- [x] **64. 訪客送單聯絡身分與私人管理呈現**（Done）
  - 未登入的團購參與者與店家顧客送單時，必須擇一提供臺灣手機號碼或 Email；登入者直接使用後端驗證的 Google 帳號，不重複要求聯絡資料。
  - 聯絡身分只顯示在具權限的團購／店家管理頁與 Excel；公開菜單、分享頁及 QR Code 不揭露。舊訂單維持可讀並標示未記錄，不改寫歷史資料。
  - 驗收：缺少或無效訪客聯絡資料會被後端拒絕；手機與 Email 正規化正確；登入送單、舊訂單、訂單核價、Excel、公開頁與兩種點餐流程皆通過回歸測試。此階段不加入 SMS／Email OTP，因此只增加可追溯性與基本嚇阻。
  - 對應：FR-53、AC-35。

- [x] **65. 建立雙模式送單防重複、速率限制與機器人誘捕**（Done）
  - 團購與店家固定菜單共用後端保護：30 秒相同訂單只建立一次；同一來源與身分對同一菜單 10 分鐘最多完成 5 次建立或更新；隱藏誘捕欄位被填寫時拒絕送單。
  - 保護資料只在單一服務記憶體保存不可逆雜湊與短時間戳，不記錄原始 IP、手機、Email、Token、姓名或訂單內容；建立失敗時撤銷本次保留，不誤耗一般使用者次數。
  - 驗收：兩種模式的重複送單回傳 409、第 6 張回傳 429 與 `Retry-After`、誘捕欄位回傳 422；完整既有回歸測試維持通過。
  - 對應：FR-54、AC-36。

- [x] **66. 同一團購身分只保留一張可加購或修改的訂單**（Done）
  - 同一 Google 帳號或同一訪客聯絡方式在同一團購再次送單時，先詢問加購、修改或取消；加購與修改都更新原訂單並保持原編號。
  - 訪客更新需同時持有第一次送單保存在原瀏覽器的私密憑證，不能只靠手機或 Email 冒用；登入者可由後端驗證帳號跨裝置更新。
  - 店家固定菜單維持可重複下單，只沿用既有短時間防重複與速率限制，避免阻擋隔日回購。
  - 驗收：同身分加購及修改後管理頁仍只有一張訂單；不同備註不誤合併；新裝置無憑證不可修改；不同身分仍可各自送單；既有訂單、Excel 與資料庫 schema 不受影響。
  - 對應：FR-55、AC-37。
