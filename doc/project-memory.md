# AI 菜單點餐系統專案記憶

## 1. 文件用途與更新原則

本文件保存 `ai-menu-ordering` 專案已確認且需要跨工作階段延續的資訊，供開發代理與使用者在開始下一個小任務前快速掌握方向。

- 權威來源依序為：使用者目前明確指示、`AGENTS.md`、`doc/requirements.md`、本文件、`doc/todo.md`、相關規格文件與已驗證的實作結果。
- 只有在決策被確認、既有決策改變、重要限制被發現，或任務通過實際驗收時才更新。
- 不記錄猜測、未驗證成果、完整除錯過程或任何機密值。
- 更新目前進度時，只加入已完成且已測試的事實，不得預先宣告後續任務完成。

最後整理日期：2026-08-10。

## 2. 專案最終成果

1. 使用者上傳一張清楚的 JPG、PNG 或一頁式 PDF 菜單，由 AI 擷取分類、品項名稱與價格；團購主揪鎖定辨識內容並勾選本次提供品項，店家模式才提供菜單內容維護。
2. 一般使用者團購模式中，統籌建立團購代碼並分享給參與者。
3. 每位參與者輸入姓名、選擇餐點與數量，送單後取得個人訂單編號並查看自己的訂單。
4. 統籌使用獨立管理連結查看所有個人明細、餐點彙整及總金額，關閉團購後複製摘要向店家訂購。
5. 團購模式完成後增加店家固定菜單模式：店家預先設定菜單，顧客透過固定網址或 QR Code 點餐。
6. 統籌與店家從後台依姓名查看餐點，明確辨識每份餐點屬於誰；訂單編號仍由系統保存，但不放在個人明細標題。
7. 兩種模式共用菜單、購物車、訂單與管理畫面核心，但菜單與訂單資料彼此正確歸屬，不使用全站唯一正式菜單。

## 3. 已確認技術與安全決策

- 前端使用 HTML、CSS、JavaScript，不使用 React 或 Vite。
- 後端使用 Python 與 FastAPI。
- 本機資料庫使用 SQLite。
- 使用 Git 與 GitHub 進行版本管理。
- 最後部署至 Render。
- 本機維持 SQLite；Render 正式環境已確認使用 PostgreSQL，透過 `DATABASE_URL` 連線並從空資料庫開始。
- AI API 只能由後端呼叫；API Key 不得出現在 HTML、前端 JavaScript、瀏覽器畫面或前端打包檔案。
- API Key、密碼、Token、Cookie、Session Secret 與正式資料庫連線字串不得寫入程式碼或提交至 Git，必須透過環境變數讀取。
- Render 機密資料必須放在 Render Environment Variables，不得寫入 Repository。
- `.env.example` 只能包含變數名稱與安全示範值；不得建立或提交真正的 `.env`。
- 菜單上傳使用 `python-multipart` 接收瀏覽器檔案，並使用 `pypdf` 檢查 PDF 是否剛好一頁；兩者皆只由後端使用。
- 第一版單一菜單檔案大小上限已確認為 10 MB。
- 為避免專案產生額外 API 費用，AI 菜單辨識改用 Google Gemini Developer API 免費層，預設模型為 `gemini-3.6-flash`；免費額度有使用限制，模型與條款仍須在部署前再次確認。
- JPG、JPEG、PNG 與一頁式 PDF 以原始 bytes 和正確 MIME type 傳給 Gemini，不使用永久檔案上傳。
- AI 回應必須使用 JSON Schema 結構化輸出，契約位於 `doc/menu-recognition.schema.json`；看不清楚的名稱或價格使用 `null` 並交由店家修正，不得猜測。
- Gemini 免費層提交的內容可能用於改善 Google 產品，因此現階段只處理公開且不含機密資訊的餐廳菜單。

## 4. 菜單資料決策

- 所有人工建立或 AI 辨識的菜單，都必須轉換為 `doc/menu-data-spec.md` 定義的統一 JSON 格式。
- `data/menu.json` 保留為開發與未串接 AI 時的示範菜單，不再視為團購模式與店家模式共同覆寫的正式資料位置。
- 點餐頁面只讀取統一菜單資料，不因不同菜單圖片或版面重新製作網站。
- 分類與餐點必須依當次確認後的菜單資料動態產生，不得寫死為飯類、麵類或任何固定分類；目前的飯類與麵類只供第一階段測試。
- 菜單包含餐廳名稱、分類，以及餐點的識別碼、名稱、說明、非負整數價格與供應狀態。
- 團購菜單屬於單一團購並保存為快照；店家固定菜單屬於店家。菜單歸屬、團購狀態與管理 Token 由資料庫保存，不放進菜單內容 JSON。
- 團購代碼是參與者公開入口；統籌管理連結使用不可猜測的私密 Token；個人訂單編號用來辨識每位參與者或顧客的餐點，三者不可混用。
- 訂單必須保存送單當下的品項名稱與單價，避免菜單更新改變歷史訂單。
- 第一版資料格式不支援糖度、冰量、加料、尺寸、套餐或其他複雜客製選項。

## 5. 開發順序

### 第一階段：點餐底座

固定 `menu.json` → 菜單顯示 → 購物車 → 訂單表單 → FastAPI → SQLite → 管理後台。

第一階段必須在未串接 AI 時仍可獨立使用。

### 第二階段：AI 菜單匯入（已完成至人工確認）

菜單檔案上傳 → AI 辨識 → 團購勾選供應品項／店家維護菜單。

### 第三階段：一般使用者團購模式（優先）

團購資料格式 → 團購資料表 → 建立團購代碼與管理連結 → 參與者送單與個人訂單 → 統籌個人明細與餐點彙整 → 關閉團購與複製摘要 → 整合驗收。

### 第四階段：店家固定菜單模式

店家固定菜單 → 固定網址與 QR Code → 顧客個人訂單 → 店家訂單後台 → 雙模式整合驗收。

### 第五階段：發布前操作體驗補強

雙模式首頁 → 每個餐點的自由文字備註 → 全站返回與首頁導覽 → 店家固定菜單分類與品項增刪。

### 第六階段：發布準備

測試 → GitHub → Render 與正式部署。

## 6. 目前進度

- 已建立 README、需求規格、開發待辦與菜單資料規格等專案文件。
- 已初始化 Git Repository，尚未由開發代理執行 commit 或 push。
- 已完成 `.gitignore`、`.env.example` 與 `AGENTS.md` 的機密資訊安全設定，且沒有建立真正的 `.env`。
- 已建立並使用 Python 內建 `json` 模組驗證 `data/menu.json`。
- 已建立 `doc/menu-data-spec.md`，說明統一菜單資料格式。
- Todo 第 2、3 項已完成：已建立 HTML、CSS、JavaScript 菜單頁面，並能讀取及顯示 `data/menu.json`。
- 已使用 localhost 實際讀取 `data/menu.json`，成功顯示 2 個分類與 6 個餐點，桌面及手機版面測試通過。
- Todo 第 4 項已完成：可供應餐點可獨立增加或減少數量，數量不低於 0，停售品項不可選取。
- Todo 第 5 項已完成：購物車會即時顯示已選品項、單價、數量、小計與總金額，品項歸零時會從明細移除。
- Todo 第 6 項已完成：已建立最小 FastAPI 後端，可由根網址導向既有點餐頁面，並提供 `frontend/` 靜態資源與 `data/menu.json`。
- 已使用 HTTP 請求與實際瀏覽器驗證 FastAPI 服務；點餐頁面成功顯示 2 個分類、6 個餐點及既有購物車。
- Todo 第 7 項已完成：已定義訂單與訂單餐點資料格式，並建立 `POST /api/orders`；後端會驗證姓名、購物車、數量、小計與總金額，並為有效訂單補上 UTC 建立時間。
- 已實際測試 1 組有效訂單及 5 組無效訂單；有效資料回傳 HTTP 201，缺少姓名、空購物車、無效數量、小計不一致與總金額不一致皆回傳 HTTP 422。
- Todo 第 8 項已完成：已建立 SQLite `orders` 訂單主表、`order_items` 訂單明細表、外鍵與必要索引，並提供可重複執行的初始化方式。
- 已使用全新暫存資料庫測試重複初始化及寫入一張訂單與兩筆明細；顧客姓名、數量、總金額、明細關聯與外鍵設定皆驗證通過。
- Todo 第 9 項已完成：`POST /api/orders` 會將通過驗證的訂單主表與所有餐點明細，以單一交易完整寫入 SQLite，並回傳資料庫訂單編號。
- 已實際透過 API 儲存一張含兩筆明細的訂單，並從 SQLite 查到顧客姓名、數量 `[2, 1]` 與總金額 240；另已驗證明細寫入失敗時不會留下主表或部分明細。
- Todo 第 10 項已完成：購物車已加入取餐姓名欄位、送出按鈕、送出中狀態及成功或失敗訊息，可將目前購物車送至 `POST /api/orders`。
- 已使用實際瀏覽器驗證空購物車不可送出、缺少姓名會提示並聚焦輸入欄、有效訂單會儲存並清空購物車，以及快速重複點擊不會產生重複訂單。
- Todo 第 11 項已完成：已建立 `GET /api/admin/orders`，可依建立時間由新到舊回傳多張訂單，包含訂單編號、顧客姓名、建立時間、總金額及完整餐點明細。
- 已實際呼叫管理查詢 API 驗證 2 張訂單，回傳 HTTP 200，總金額、餐點筆數與各品項數量皆正確；空資料庫回傳空陣列的測試亦通過。
- Todo 第 12 項已完成：已建立 `/admin` 簡易管理後台，會依建立時間由新到舊顯示訂單編號、顧客姓名、台北時間、總金額及完整餐點明細。
- 已使用實際瀏覽器驗證 2 張訂單與 3 筆餐點正確顯示、中文資料與金額正確、無水平溢出，並確認空訂單與 API 讀取失敗皆有清楚提示。
- Todo 第 13 項已完成：第一階段 AC-01 至 AC-07 全部通過；已從實際瀏覽器完成菜單顯示、選取多品項、購物車計算、姓名送單及管理後台查看訂單，並直接核對 SQLite 寫入結果。
- 第一階段點餐底座已完成，且已確認未設定 AI API Key、未串接 AI 時仍可獨立使用；驗收紀錄位於 `doc/phase-1-acceptance.md`。
- Todo 第 14 項已完成：已在 `doc/menu-upload-spec.md` 定義單一 JPG、JPEG、PNG 或一頁式 PDF 的上傳規則、驗證順序與使用者錯誤訊息；檔案驗證成功後才能進入 AI 辨識，且不得直接覆寫正式菜單。
- Todo 第 15 項已完成：已建立 `/upload` 店家上傳頁面與 `POST /api/menu-uploads`，可接收單一 JPG、JPEG、PNG 或一頁式 PDF，並驗證格式、內容特徵、可讀性、10 MB 上限及 PDF 頁數。
- 已以 12 項自動測試、實際 HTTP 請求及瀏覽器驗收上傳流程；一頁 PDF 可成功通過，錯誤格式、空檔、格式不符、損壞圖片、超過 10 MB、多頁 PDF、缺少檔案與一次多檔皆會被拒絕。
- 上傳成功目前只代表檔案通過基本驗證，不會永久保存、不會修改 `data/menu.json`，也尚未呼叫 AI。
- Todo 第 16 項文件已配合免費方案更新：`doc/ai-recognition-contract.md` 現定義 Gemini Developer API、`gemini-3.6-flash`、圖片與 PDF 輸入方式、提示原則、結構化輸出、正式菜單轉換與錯誤處理。
- 已使用 Python 內建 `json` 模組驗證辨識 Schema，確認所有物件欄位完整且禁止額外欄位，並以一份測試辨識結果成功轉換成既有 `menu.json` 結構。
- Todo 第 17 項已完成：已安裝 `google-genai==2.17.0`，後端、上傳頁面與錯誤處理已接上 Gemini；19 項離線測試全部通過。
- 已用程式產生的清楚繁體中文測試菜單完成真實 Gemini API 驗收，正確辨識「山海小館」、飲品與甜點 2 個分類、6 個品項及全部價格，`needs_review` 為 `false` 且沒有警告。
- 真實辨識前後已核對 `data/menu.json` 雜湊完全相同；辨識結果仍只是待確認資料，不會直接改寫正式菜單。
- Windows Anaconda 環境使用 `truststore` 讀取系統憑證，並強制 Gemini 非同步連線使用相容的 HTTP 傳輸，避免本機憑證驗證失敗；其他平台不套用此 Windows 專用設定。
- Todo 第 18 項已完成：AI 辨識結果會顯示為可修改表單，店家可修正餐廳名稱、分類名稱、品項名稱與非負整數價格，再按下「確認辨識結果」。
- 已使用實際瀏覽器驗證：空白價格會阻止確認、顯示欄位錯誤並聚焦；補入價格且修改分類與品項名稱後可成功確認，畫面值與確認資料一致，並清楚提示尚未儲存為正式菜單。
- 確認介面沒有瀏覽器 JavaScript 錯誤或水平溢出；本任務只在瀏覽器記憶體中保存確認結果，沒有新增儲存 API，也沒有修改 `data/menu.json`。
- Todo 第 19 項已完成：使用者確認產品採共用點餐核心的雙模式方向，先完成一般使用者團購模式，再增加店家固定菜單模式。
- 已更新需求、模式規格、菜單資料規格、README 與 Todo，正式區分團購代碼、統籌管理連結、個人訂單編號、團購菜單快照及店家固定菜單。
- 原本「確認後直接覆寫全站菜單」的 Todo 已取消，`data/menu.json` 繼續只作為示範與備援菜單。
- Todo 第 20 項已完成：已建立 `doc/group-order-data-spec.md`，定義團購、菜單快照、公開團購代碼、私密管理 Token、個人訂單編號、個人查看 Token、狀態及驗證規則。
- 公開團購代碼只用來進入團購，不代表管理權限；管理 Token 與個人查看 Token 皆使用高強度隨機值，資料庫只保存 SHA-256 雜湊，原始值只在建立成功時回傳一次。
- 團購菜單在建立時保存獨立快照；後端送單時必須依快照重新核對供應狀態與價格，不能信任瀏覽器傳入的金額。
- 已使用 Python 內建 `json` 模組成功解析規格中的完整 JSON 範例，並檢查必要安全規則與需求追溯；本任務沒有修改前端、後端、資料庫或示範菜單。
- Todo 第 21 項已完成：SQLite 已加入 `group_sessions` 與 `group_menus`，現有 `orders` 可選擇性連結團購、保存公開個人訂單編號及個人查看 Token 雜湊。
- 資料庫初始化可為既有第一階段資料庫補上團購欄位，保留原有訂單；公開代碼、管理 Token 雜湊、個人訂單編號、個人 Token 雜湊與團購外鍵皆有必要限制或索引。
- 已以全新暫存 SQLite 驗證兩個團購的菜單與訂單互不混淆、無效關聯會被拒絕，並驗證舊訂單遷移；完整測試共 22 項全數通過。
- Todo 第 22 項已完成：人工確認表單會呼叫 `POST /api/groups`，將任意分類與品項轉成統一菜單格式並建立獨立團購快照。
- 團購建立成功會回傳 6 碼公開代碼、參與者連結及只提供一次的統籌管理連結；管理 Token 位於網址片段，資料庫只保存 SHA-256 雜湊，不會寫入伺服器網址紀錄。
- 菜單分類與品項 ID 依原始順序產生為純小寫英文識別碼，不依賴飯類、麵類或中文轉音；建立團購不會修改 `data/menu.json`。
- 已驗證有效確認資料可建立 2 個分類、3 個品項的團購，無效確認資料回傳 422 且不留資料；完整測試 24 項及前端 JavaScript 語法檢查皆通過。
- Todo 第 23 項已完成：新增 `/join` 團購代碼入口、`/groups/{public_code}` 分享頁與 `GET /api/groups/{public_code}` 公開菜單 API。
- 公開 API 只回傳團購代碼、`open`／`closed` 狀態及菜單，不包含資料庫 ID、管理 Token 或管理 Token 雜湊；不存在的代碼回傳容易理解的 404 訊息。
- 已關閉團購仍可查看原菜單，但公開頁會清楚顯示已截止；頁面可動態顯示任意分類、餐點、說明、價格與暫停供應狀態。
- 已驗證正確代碼、不存在代碼、關閉團購及公開頁回應；完整測試 27 項、團購與上傳 JavaScript 語法檢查及 Git 差異格式檢查皆通過。
- Todo 第 24 項已完成：公開團購頁已加入數量調整、購物車、姓名與送單功能，使用 `POST /api/groups/{public_code}/orders` 建立團購個人訂單。
- 團購訂單請求只接受餐點 ID 與數量；後端會從該團購菜單快照重新確認供應狀態、名稱與價格，再計算小計及總金額，瀏覽器無法指定價格。
- 個人訂單編號依團購獨立產生，例如 `ABC234-001`、`ABC234-002`；個人查看 Token 只在成功回應的網址片段提供一次，資料庫只保存雜湊。
- 已驗證同團購兩人不同編號、不同團購資料隔離、停售品項、偽造價格、關閉後拒絕送單及交易流水號；完整測試 31 項與團購 JavaScript 語法檢查皆通過。
- Todo 第 25 項已完成：新增個人訂單頁與受保護的 `GET /api/groups/{public_code}/orders/{public_order_number}`，顯示餐廳、個人訂單編號、姓名、品項、數量及金額。
- 個人頁從網址片段讀取 Token，再透過 `Authorization` 標頭交給後端；公開團購代碼、個人訂單編號、缺少 Token 或錯誤 Token 都不能讀取訂單。
- 後端以固定時間方式比對 Token 雜湊，驗證成功後才回傳該張訂單，回應不含原始 Token 或雜湊。
- 已驗證正確連結、缺少與錯誤 Token、靜態頁面不預載個資；完整測試 34 項及團購與個人訂單 JavaScript 語法檢查皆通過。
- Todo 第 26 項已完成：新增統籌團購管理頁與受保護的 `GET /api/groups/{public_code}/management`，可依參與者查看該團購全部個人訂單。
- 統籌頁只從管理連結的網址片段讀取管理 Token，再透過 `Authorization` 標頭驗證；公開代碼、錯誤 Token 或個人訂單 Token 均不可取得管理資料。
- 管理 API 回應包含團購狀態及各張訂單的編號、姓名、品項、數量與金額，不包含任何 Token 或雜湊。
- 已驗證兩位參與者明細、三種未授權情況及靜態管理頁不預載訂單；完整測試 37 項與管理頁 JavaScript 語法檢查皆通過。
- Todo 第 27 項已完成：統籌管理 API 與頁面同時提供「依參與者」個人明細及「依餐點」彙整，包含訂單張數、各品項單價、總份數、品項金額及團購總金額。
- 彙整由後端依已保存的訂單明細計算，不採用瀏覽器自行提供的統計值；沒有訂單時會回傳空彙整與零元總額。
- 已以兩張訂單驗證同品項合併為 3 份、NT$ 300，並確認訂單張數與團購總額；完整測試 37 項及管理頁 JavaScript 語法檢查皆通過。
- Todo 第 28 項已完成：統籌可使用管理 Token 關閉團購，後端在資料庫保存截止時間，之後所有新訂單都會被拒絕，既有個人明細與彙整仍可查看。
- 管理 API 會依同一批已保存訂單產生純文字摘要，包含餐廳、團購代碼、狀態、訂單數、餐點總份數、個人明細與總金額；管理頁可一鍵複製，若瀏覽器不允許則自動選取文字供 `Ctrl+C`。
- 本功能不會自動聯絡店家或呼叫 LINE、Messenger、簡訊等外部平台。
- 已驗證正確與錯誤管理 Token、截止時間、關閉後拒絕送單、保留既有統計及文字摘要內容；完整測試 39 項與管理頁 JavaScript 語法檢查皆通過。
- Todo 第 29 項已完成：已從菜單上傳測試結果、建立團購、兩位參與者送單、個人查看、統籌彙整、關閉團購到截止後拒絕送單，完成一條完整自動化流程。
- AC-08 至 AC-12 全數通過；雞腿便當 3 份加紅茶 1 份的統計為 NT$ 390，與兩張個人訂單及文字摘要一致，驗收紀錄位於 `doc/group-order-acceptance.md`。
- 第一階段示範菜單送單方式仍可使用；舊的公開示範管理後台已限定只讀取非團購訂單，不會洩漏團購參與者資料。
- `data/menu.json` 在整合測試前後完全相同；整套測試 40 項、4 個團購相關 JavaScript 語法檢查及 Git 差異格式檢查皆通過。
- 一般使用者團購模式的程式功能與自動化驗收已完成；下一個任務是第 30 項「定義店家固定菜單資料格式與入口」。
- Todo 第 30 項已完成：`doc/store-menu-data-spec.md` 定義店家資料、目前菜單、更新版本、店家訂單關聯、固定公開網址、QR Code 與私密管理入口。
- 第一版介面只做單一店家情境但資料按店家 ID 歸屬；固定網址為 `/stores/{public_slug}`，菜單更新不改網址或 QR Code，且不修改團購快照或 `data/menu.json`。
- QR Code 只能包含公開網址，店家管理 Token 使用網址片段及 `Authorization` 標頭，資料庫只保存雜湊；顧客個人 Token 不具店家管理權限。
- Todo 第 31 項已完成：SQLite 已加入 `store_profiles` 與 `store_menus`，現有訂單表可選擇性連結店家，初始化時可安全保留並遷移既有示範與團購訂單。
- 人工確認頁可選擇建立團購或店家固定菜單；`POST /api/stores` 建立固定公開識別碼與一次性管理連結，`PUT /api/stores/{public_slug}/menu` 以店家 Token 更新目前菜單，`GET /api/stores/{public_slug}` 只回傳公開資料。
- 店家菜單更新會增加版本但不改固定公開網址；舊訂單明細保留更新前名稱與價格，錯誤管理 Token 不得更新，`data/menu.json` 不受影響。
- 已驗證建立、公開讀取、從第 1 版更新為第 2 版、權限拒絕、既有訂單快照及舊資料庫遷移；完整測試 42 項、4 個 JavaScript 語法檢查及 Git 差異格式檢查皆通過。
- Todo 第 32 項已完成：`/stores/{public_slug}` 會顯示該店家目前固定菜單，`/api/stores/{public_slug}/qr.svg` 由後端使用使用者已批准的 `qrcode 8.2` 產生 SVG QR Code，不依賴外部服務。
- QR Code 只包含依目前網站來源組成的完整公開網址，不含 Token、查詢參數、網址片段或管理資料；兩個不同店家網址的菜單不會互相混淆。
- 已使用實際瀏覽器驗證公開頁顯示 2 個分類、6 個餐點，QR 圖檔載入完成且沒有瀏覽器錯誤；完整自動測試 46 項、相關 JavaScript 語法檢查及 Git 差異格式檢查皆通過。
- 下一個任務是第 33 項「建立店家顧客個人訂單流程」。
- Todo 第 33 項已完成：店家固定頁已加入動態購物車、數量調整、取餐姓名與送單流程，店家暫停接單時不顯示送單表單。
- 店家訂單由後端依目前菜單重新核價，編號格式為 `S-{完整店家識別碼大寫}-{流水號}`；送單成功後以網址片段提供一次性個人 Token，資料庫只保存雜湊。
- 顧客個人查看頁同時支援團購與店家模式；正確 Token 只可查看自己的訂單，錯誤 Token、跨店存取、未知餐點及暫停接單都會被拒絕。
- 已驗證兩位顧客取得不同編號與正確餐點，完整測試 50 項及店家頁、個人訂單頁 JavaScript 語法檢查皆通過。
- 下一個任務是第 34 項「建立店家訂單後台」。
- Todo 第 34 項已完成：`/stores/{public_slug}/manage#token=...` 提供店家私密訂單後台，顯示每張訂單的編號、姓名、餐點、數量、金額與送單時間。
- 店家管理 API 只依店家管理 Token 回傳該店訂單，不回傳任何 Token 或雜湊；錯誤 Token、其他店家 Token及顧客個人 Token 都會被拒絕，其他店家的訂單不會混入。
- 完整測試 53 項與店家頁、個人訂單頁、店家管理頁 JavaScript 語法檢查皆通過。
- 下一個任務是第 35 項「執行店家模式與雙模式整合驗收」。
- Todo 第 35 項已完成：`tests/test_store_flow.py` 以暫存 SQLite 完整驗證菜單上傳測試回應、建立店家、公開菜單、QR Code、兩位顧客送單、個人查看、店家後台及團購並存流程。
- AC-13 至 AC-15 全數通過；兩位店家顧客合計 NT$ 390，店家後台可依編號與姓名正確辨識，小美與小華的餐點皆正確。
- 整合測試曾發現舊示範後台會誤讀店家訂單；已將查詢修正為同時排除 `group_session_id` 與 `store_profile_id`，避免店家及團購顧客姓名外洩。
- 店家、團購與舊示範三種訂單已確認互不混淆，`data/menu.json` 前後完全相同；驗收紀錄位於 `doc/store-mode-acceptance.md`。
- 完整自動測試 54 項與全部 8 個前端 JavaScript 檔案語法檢查皆通過。
- 店家固定菜單模式已完成本機 MVP 驗收；下一階段涉及 GitHub、Render 與正式資料庫選擇，必須先取得使用者批准，不得自行 commit、push 或部署。
- 使用者確認發布前需補強三項操作：雙模式首頁、所有主要頁面的返回與首頁導覽，以及店家固定菜單的分類與品項增刪。
- 團購模式不提供菜單分類或品項增刪；兩種模式都讓使用者針對每個已選餐點填寫選填自由文字備註。
- Todo 第 36 項已完成：首頁清楚分為「團購點餐」與「店家固定菜單」，提供建立新團購、輸入代碼加入及建立店家菜單三個入口；兩種建立入口都進入相同上傳辨識核心，但只顯示對應模式。
- 早期固定 `data/menu.json` 點餐頁已移至 `/demo`，不再作為系統首頁且仍可獨立使用。
- 已使用實際瀏覽器驗證桌面與 390px 手機寬度：兩張模式卡與三個入口皆可讀取，沒有水平溢出；實際點擊團購與店家入口後，網址及模式標題正確且沒有瀏覽器錯誤。
- 完整自動測試 58 項與 `frontend/upload.js` 語法檢查通過；下一個任務是第 37 項「補齊主要頁面的返回與首頁導覽」。
- 使用者明確優先確認 Todo 第 38 項：AI 辨識後應顯示菜單上的實際分類、菜名與單價，點餐者只調整數量並可為各餐點填寫如「小辣」或「半糖少冰」的備註。
- Todo 第 38 項已完成：團購及店家點餐頁都提供每餐點最多 200 字的自由文字備註；備註保存在訂單明細，個人訂單、團購統籌後台、店家後台及團購文字摘要均可看到。
- 備註不修改 AI 辨識出的單價，也不建立固定糖度、冰量、辣度、加料、尺寸或套餐選單與自動加價；辨識確認表單已移除「品項 1、2」編號標籤，保留實際辨識名稱與價格供人工確認。
- SQLite 初始化會為既有 `order_items` 安全補上空白 `note` 欄位，不影響舊訂單；完整自動測試 63 項與所有前端 JavaScript 語法檢查皆通過。
- 下一個未完成任務仍是第 37 項「補齊主要頁面的返回與首頁導覽」。
- 使用者回報實際飲料菜單的價格仍全部需要手動輸入。檢查原圖後確認其每項均有 L／XL 兩個價格，而舊 AI 規則會刻意將多價格設為 `null`，這是需求與實作不符的直接原因。
- Todo 第 36A 項已完成：AI 規則改為依餐點列與 L／XL、大小份等欄位配對原價，並拆成「品項（規格）」的獨立可點項目；只有原圖真的無法辨識才允許價格為空。
- 首頁已依角色重整：主揪使用「上傳菜單，建立團購」，參與者直接在首頁輸入 6 碼代碼，店家另有固定菜單入口；頁面並明列「上傳、辨識、分享、點餐、統計」流程。
- 團購建立完成頁會明確告知主揪分享參與連結或代碼，提供查看點餐頁、複製代碼與進入私密管理頁三個操作。
- 已對照 DiiN 的上傳菜單、AI 擷取品項與價格、分享連結及自動統計流程；本機瀏覽器已驗證新版首頁、主揪入口及代碼格式錯誤提示。完整自動測試 65 項及所有前端 JavaScript 語法檢查通過。
- 以使用者原始 `菜單.png` 呼叫 Gemini 的實際重辨識測試遇到 `AI_SERVICE_UNAVAILABLE`，因此不能宣稱該次實際價格辨識已成功；需由使用者在正常本機服務重新上傳同一圖片完成最終驗收。
- 使用者確認團購建立規則改為：AI 辨識後的名稱與價格不可在團購確認頁修改，主揪只勾選這次要提供的品項；若辨識錯誤則改用清楚圖片重新辨識。店家固定菜單仍保留後續維護能力。
- Todo 第 36B 項已完成：團購與店家點餐頁改為緊湊列式，餐點數量為 0 時隱藏備註欄，按下增加數量後只展開該品項備註，減回 0 時清除並收起。
- 團購彙整改用餐點識別碼與備註共同分組；同一餐點的「半糖少冰」與「無糖去冰」會分成兩列，數量與金額各自計算。
- 團購統籌與店家後台的個人明細標題只顯示點餐者姓名，不再附加訂單代碼；個人私密訂單頁仍保留自己的訂單編號。
- 已使用暫存資料實際驗證同餐點兩種需求分列為 1 份與 2 份、個人明細只顯示 Amy 與 Ben；亦發現並修正 CSS 讓隱藏備註失效的問題。完整自動測試 66 項通過。
- Todo 第 36C 項已完成：團購菜單確認頁改為緊湊勾選清單，可依品項名稱、分類與最高價格篩選，並批次勾選或取消符合條件的品項；沒有對應後續頁面的「1／3」標示已移除。
- 首頁三個主要入口已移到頁面最上方；店家菜單確認頁會將名稱相同且帶 L／XL 規格的項目合併為同一列呈現，仍分別保存各規格價格。
- 團購管理頁會把同品項的不同備註需求排列於同一品項卡內，並可下載含「餐點彙整」與「個人明細」兩張工作表的 Excel 檔；Excel 不含管理 Token。
- 已以實際瀏覽器驗證首頁按鈕與團購彙整分組，並使用試算表工具匯入及預覽兩張 Excel 工作表；中文、數量與金額正確。完整自動測試 70 項及所有前端 JavaScript 語法檢查通過。
- 依使用者最新確認，首頁不再放一列重複的頂部角色按鈕；8 個主要操作分頁改為只在頁首顯示一個「首頁」按鈕，正常首頁移除固定示範菜單連結。Todo 第 37 項已完成。
- 上傳頁已新增 JPG、PNG 圖片預覽與矩形範圍裁切，可在辨識前選出實際菜單區域；一頁式 PDF 第一版提供整頁預覽但暫不裁切。
- 團購菜單的 L／XL 同品項合併成同一列，各規格仍可分別勾選；名稱、分類或最高價格篩選會同步取消不符合品項，因此參與者只會看到主揪最後保留的品項。
- Excel 下載已補上訂單筆數、總金額及內容長度檢查；沒有訂單時停用下載。已用含一張訂單的實際 API 檔驗證兩張工作表都有明細，並確認無公式錯誤。
- 本次完整自動測試 72 項與全部 9 個前端 JavaScript 檔案語法檢查通過；下一個未完成任務是第 39 項「建立店家固定菜單分類與品項增刪」。
- 使用者提出系統未來可從菜單點餐延伸為一般多人選品與訂單彙整工具；目前仍以菜單點餐為主，不在此階段更名、擴充通用商品欄位或改變既有流程。
- 團購及店家公開點餐頁已將同品項 L／XL 合併在同一列，每個規格仍可分別調整數量並填寫各自備註。
- Todo 第 39 項已完成：店家菜單確認頁可新增、刪除分類與品項；已實際操作新增與刪除後保存，公開固定菜單與確認結果一致。
- Excel 下載前會先重新取得最新訂單，後端回傳訂單筆數供前端核對，並修正工作表 XML 順序以提高 Microsoft Excel 相容性。已用 2 張訂單、3 筆餐點、總金額 NT$145 的實際下載檔逐格檢查與預覽，「餐點彙整」及「個人明細」都有完整資料且無公式錯誤。
- Todo 第 40 項已完成：團購 Excel 新增「訂購者合計」工作表，每張送出的訂單各列一筆姓名、餐點總數量、合計金額與送單時間；同名的不同訂單維持分列，不會誤合併。「餐點彙整」與「個人明細」仍保留。已用兩張訂單的實際 XLSX 逐格檢查及預覽三張工作表，訂購者金額 NT$70、NT$75 與團購總額 NT$145 一致，完整自動測試 74 項通過。
- Todo 第 41 項已完成：店家訂單後台新增依品項及備註需求彙整，並保留個別顧客訂單；可下載含「餐點彙整」、「顧客合計」與「顧客明細」三張工作表的 Excel。已用兩張訂單驗證少飯 2 份 NT$160、一般 1 份 NT$80 與總額 NT$240 一致，三張工作表皆有完整資料且無公式錯誤；完整自動測試 75 項與 9 個前端 JavaScript 語法檢查通過。
- 店家後台曾因新版 HTML 與瀏覽器快取中的舊版 JavaScript 混用，造成已有訂單時彙整空白且 Excel 按鈕仍停用；已為店家後台 CSS 與 JavaScript 加入版本記號，強制瀏覽器取得同一版資源。已在實際瀏覽器用 2 張訂單、總額 NT$100 驗證彙整正確顯示、下載按鈕可用、Excel API 回傳 HTTP 200，完整自動測試 75 項通過。
- Todo 第 42 項已完成：當 AI 未辨識到餐廳名稱時，團購與店家確認頁會開放餐廳名稱欄供手動輸入，空白時顯示明確錯誤並阻止建立；AI 已辨識名稱時，團購模式仍鎖定名稱，餐點名稱與價格規則不變。完整自動測試 76 項與 9 個前端 JavaScript 語法檢查通過。
- Todo 第 43 項已完成：Gemini 3.6 Flash 使用 `low` thinking level；一般 429、5xx、HTTP 逾時與傳輸錯誤最多重試 2 次，分別等待約 1 秒、2 秒。401／403、每日額度用完、AI 拒絕及無效輸出不重試，單次逾時仍為 60 秒。指定辨識測試 11 項及完整自動測試 79 項全數通過。
- Todo 第 43A 項已完成：每次 Gemini 呼叫會在後端 console 記錄 attempt、開始時間、成功或失敗、單次耗時，以及 APIError HTTP code 或安全錯誤類型；不記錄錯誤原文、Key、Token、圖片或個資。OSError 已納入最多 2 次的暫時性網路錯誤重試。
- Todo 第 44 項已完成：新增共用 `menu-variants.js`，從品項名稱末尾的全形或半形括號拆出 base item 與 variant，不限 L／XL；同一 base 至少有兩個規格才合併呈現。團購確認、店家確認、團購點餐與店家點餐共用同一判斷，各 variant 的 item id、完整名稱、價格、供應／勾選狀態、數量與備註都保持獨立。
- 已驗證「煎蛋熱壓吐司（白吐司／全麥）」、「紅茶（M／L）」會各合成一個畫面群組，「薯餅」維持普通單品；完整自動測試 80 項及 4 個相關 JavaScript 語法檢查通過。
- Todo 第 43B 項已完成：Gemini 每日額度判斷改為只接受明確的 PerDay／Daily quota ID；`FreeTier` 不再被誤當成每日額度，RPM／TPM 429 會先採用 Gemini `RetryInfo.retryDelay` 或 HTTP `Retry-After`，沒有供應商資訊才等待約 1 秒、2 秒。
- 以使用者的代表性真實 `菜單.png` 在可連線環境執行 5 次 Gemini 呼叫，全部在第一次 attempt 成功，單次 API 約 11.3～12.0 秒，未重現 429、500、503、逾時或 SDK 錯誤；因此不能倒推先前偶發失敗的真實 HTTP code。受限制的測試沙箱曾穩定重現 `TransportError`，確認是該診斷環境無法連外，不代表使用者本機或 Gemini 服務故障。
- 本次再次驗證共用規格邏輯及四個指定頁面，不需修改前端：吐司白吐司／全麥、紅茶 M／L 正確合併，薯餅維持單品。完整自動測試 82 項通過，團購、店家、訂單、後端核價、歷史快照及 Excel 皆未受影響。
- Todo 第 45 項已完成：Render 使用 Python 3.12、Build Command `pip install -r requirements.txt`，Start Command 從 `backend.main:app` 啟動並信任 Render proxy headers；FastAPI 直接提供前端與資料靜態資源，QR Code 依正式 HTTPS request origin 產生，Excel 全程在記憶體建立。
- Todo 第 46 項已完成：新增小型 SQLite／PostgreSQL 相容層與 PostgreSQL 空庫初始化，不重寫既有產品資料函式；正式送單使用列鎖保護團購及店家流水號，SQLite 原有交易、舊資料升級及全部測試維持不變。
- 已安裝並固定 `psycopg[binary]==3.3.4`；Python 91 項測試、10 個前端 JavaScript 語法、套件相依、Python 編譯及真實 Uvicorn HTTP 啟動檢查通過。沒有真正 `.env`、Git commit 或 remote，機密掃描只命中測試中的安全假值。
- Todo 第 47 項仍未完成：必須由使用者先建立或批准 Private GitHub Repository，之後才能 commit、push、建立 Render PostgreSQL／Web Service，並以真正 PostgreSQL 與公開網址執行完整正式驗收。

`doc/todo.md` 是任務狀態的正式清單；每次只能更新本次通過驗收的對應項目。

## 7. 目前不做

- 會員登入。
- 多店家帳戶與多分店管理平台；店家模式第一版只支援單一店家情境。
- 外送與派單。
- 線上付款。
- 電子發票。
- LINE 通知。
- 自動將訂單傳送到 LINE、Messenger 或簡訊；團購第一版只提供可複製的文字摘要。
- POS 串接。
- 複雜套餐。
- 固定的加料、大小杯、冰量、糖量選單與自動加價；自由文字餐點備註已支援。
- 保證辨識所有複雜或手寫菜單。

## 8. 使用者背景與協作方式

- 使用者第一次完整製作 Web 系統；說明應使用繁體中文與白話。
- 優先使用課程已學過且已確認的技術。
- 每次只進行一個可獨立測試、可明確驗收的小任務。
- 修改前先說明本次目的、影響範圍、預計修改檔案與驗收方式。
- 不可一次產生整套系統，也不可自行增加未確認功能。
- 每次完成後提供實際測試指令、結果與使用者自行測試方式，然後停止等待確認。

## 9. 本機環境備註

一般 PowerShell 的 `python` 指令目前未連到 Anaconda。需要執行 Python 時，使用：

```text
C:\Users\sharo\anaconda3\python.exe
```

不得因此重新安裝 Python 或修改系統環境設定，除非使用者另行批准。

## 10. 待後續確認

- 以代表性菜單量測 Gemini 辨識正確率與延遲，並在部署前重新確認免費層額度、資料使用條款及模型是否仍可用。
- Render PostgreSQL 建立後，必須使用真正 Internal Database URL 完成空庫初始化、完整雙模式流程與重啟持久性驗收。
